import React from 'react'
import { filterData } from '../data'

const Filter = (props) => {
  return (
    <div>
        {
            filterData.map( (data) => (
                filterData={}
            ))
        }
    </div>
  )
}

export default Filter
